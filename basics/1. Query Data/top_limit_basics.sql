/*
TOP or LIMIT both have the same job they restrict the number of rows returned
here is a Query Example
SELECT TOP 3
* FROM customers

In the example above we can see that we are selecting the top 3 columns from the customers table

*/
SELECT * FROM customers

SELECT TOP 3 * 
FROM customers